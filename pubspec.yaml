name: tiny_types
description: Small, focused utility types for Dart, including Option, Unit, and other lightweight building blocks.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.5
