name: tiny_types
description: >-
  Small, focused types for Dart with explicit invariants and practical APIs.
  Includes Option, Unit, and non-empty collections.
version: 1.0.0
repository: https://github.com/hoc081098/tiny_types

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.7.0

dev_dependencies:
  built_collection: ^5.1.1
  collection: ^1.17.0
  lints: ^3.0.0
  test: ^1.25.5
